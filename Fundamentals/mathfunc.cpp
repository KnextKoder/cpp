#include <iostream>
#include <cmath>

int main () {

    double x = 3;
    double y = 4;
    double pi = 3.14;
    double z;

    // z = std::max(x, y); //max returns the maximum from a comparison of 2 nums
    // z = std::min(x, y); //min returns the minimum from a comparison of 2 nums
    // z = pow(x, y); // pow takes 2 numbers, and makes the 2nd the power of the first
    // z = sqrt(y); // finds the square root of a number
    // z = abs(-10); returns the absolute value of a number
    // z = round(pi); // rounds up or down for a given value
    // z = ceil(pi); // rounds up for a given value
    // z = floor(pi); // rounds down for a given value

    std::cout << z ;
    return 0;
}